# A document with escaped characters

This paragraph has \_underscores\_ that should NOT be italicized. The backslash escapes them.

This one has \*asterisks\* that should NOT be bold.

But this paragraph has *real* emphasis and **real** bold. Those should be rendered.

And here is some `inline code` which should remain literal.
